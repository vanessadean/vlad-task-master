<%= form_for(@task, :html => { id: 'add_task' }) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>Add a new task:</p></br>

  <%= f.collection_select, :id, List.all, :id, :title %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, id: 'task_description' %>
  </div>
  <div class="field">
    <%= f.label :priority %>
    <%= f.text_field :priority, id: 'task_priority'%>
  </div>

  <%= f.hidden_field :list_id, :value => @list.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
